# Contributing

1. 在新分支中修改代码。
2. 不要提交原始雷达数据、日志、设备地址或其他敏感信息。
3. 修改公共算法后运行 `tests/run_tests.m`。
4. 新增配置时同时更新 `docs/parameters.md` 和 README。
5. MATLAB 函数名必须与文件名一致。
6. 新增参数应提供单位、默认值和物理含义。
7. 提交 Pull Request 时说明算法变化、预期影响和验证数据范围。
